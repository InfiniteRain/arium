for do
  print(
    if false then
      break
    elseif false then
      50 
    elseif true then
      break
    else
      "str"
    end
  )
end

for do
  print(
    if false then
      50
    elseif false then
      break
    elseif true then
      "str"
    else
      40
    end
  )
end

for do
  print(
    if false then
      break
    elseif false then
      "str"
    elseif true then
      50
    else
      40
    end
  )
end

for do
  print(
    if false then
      break
    elseif false then
      "str"
    elseif true then
      break
    else
      40
    end
  )
end

/// test(.sema)
/// sema_err(line(9), {.unexpected_expr_type = {.expected = {.type_int}, .actual = .type_string}})
/// sema_err(line(21), {.unexpected_expr_type = {.expected = {.type_int}, .actual = .type_string}})
/// sema_err(line(35), {.unexpected_expr_type = {.expected = {.type_string}, .actual = .type_int}})
/// sema_err(line(51), {.unexpected_expr_type = {.expected = {.type_string}, .actual = .type_int}})

